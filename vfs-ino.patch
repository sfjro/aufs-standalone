SPDX-License-Identifier: GPL-2.0

diff --git a/fs/inode.c b/fs/inode.c
index 6b4c77268fc0..53797af5205c 100644
--- a/fs/inode.c
+++ b/fs/inode.c
@@ -1140,6 +1140,8 @@ unsigned int get_next_ino(void)
 	unsigned int *p = &get_cpu_var(last_ino);
 	unsigned int res = *p;
 
+start:
+
 #ifdef CONFIG_SMP
 	if (unlikely((res & (LAST_INO_BATCH-1)) == 0)) {
 		static atomic_t shared_last_ino;
@@ -1152,7 +1154,7 @@ unsigned int get_next_ino(void)
 	res++;
 	/* get_next_ino should not provide a 0 inode number */
 	if (unlikely(!res))
-		res++;
+		goto start;
 	*p = res;
 	put_cpu_var(last_ino);
 	return res;
